# CalSight Handoff — 2026-07-03

Audit of repo, branches, PRs, and issues, organized as a work queue. Written for the implementing agent; treat priority order as binding unless Jeff redirects.

## Current state

- `main` = `a03a554` (#347), clean, deployed. Prod auto-deploys from main (single environment, no staging).
- Everything through #347 is merged: distribution filter-awareness (#345), AI-dialog focus trap (#346), freshness fix (#344), ETL OOM + run-etl env fixes (#341/#342), zombie-run clearer (#347).
- **Two open PRs, both CI-fully-green, both mergeable, both based on the current main tip:**

| PR | Branch | Scope | Size |
|----|--------|-------|------|
| #348 | `claude/project-audit-grhhmp` | Full project audit (`docs/audit-2026-07-01.md`) + remediation of **all 5 criticals and all 22 highs** across 4 waves. 703 backend + 576 frontend tests pass. | +3349/−383, 62 files |
| #349 | `claude/data-storytelling` | Street-level lens: `/api/intersections` + `/api/corridors`, `get_top_intersections` AI tool, Stats-page IntersectionsPanel, ped/cyclist mode filters, neutral-storytelling groundwork (`docs/data-storytelling-plan.md`). | +2747/−6, 28 files |

- **The PRs overlap in 3 files**: `backend/app/ai_tools.py`, `frontend/src/components/map/LayersPanel.tsx`, `LayersPanel.test.tsx`. Merge order matters.
- No other branches exist (local or remote). Branch hygiene is clean.

## Work queue

### P0 — Ship what's already built

1. **Merge #348 first** (it's the safety/correctness PR). Jeff admin-merges. After merge: watch the deploy, verify prod health (site up, `/api/health`, spot-check a dashboard).
2. **Update #349 onto the new main**: merge main in (expect conflicts in the 3 overlap files — resolution should keep both PRs' additions; both add to the AI tool registry and LayersPanel), get CI green again, then merge.
3. **Post-merge verification for #349 in prod**: hit `/api/intersections?county=...` and `/api/corridors` against the real 11M-row dataset — the PR checklist explicitly leaves this open. Watch query latency; these are new aggregation paths with no matview behind them.

### P1 — Operational follow-ups (mostly prod-host / Jeff-box; agent can prep, Jeff executes per access boundary)

- **M-I1**: set `ETL_DATABASE_URL` in `/opt/calsight/backend/.env` so the read-only API role from #348 is actually enforced (deploys now preserve .env).
- **C4 end-to-end**: run one real `pg_dump` + `pg_restore` on the prod host to confirm the rebuilt backup path.
- **Verify prod activation of Sentry + dead-man's-switch** (#322): `grep SENTRY_DSN /opt/calsight/backend/.env`, `grep HEARTBEAT_URL ...`, `systemctl status calsight-etl-scheduler`. Code shipped long ago; activation never confirmed.
- **Run `clear_stale_runs`** (shipped in #347) to clear the 3 zombie `etl_runs` rows.
- **ZAP**: apply the `/api/schools` PII alert-filter on Jeff's box (documented false positive).
- **og.calsight.org** is NXDOMAIN — DNS record needed (Cloudflare).
- After #348 merges, **tick the completed boxes in issue #322** (backup verification, etc. are now code-done) and note which remain activation-only.

### P2 — Medium backlog (from `docs/audit-2026-07-01.md`, post-#348)

All criticals/highs are done in #348. Remaining, in the audit's recommended order:

- AI-answer-correctness leftovers if any survive #348 (M-B5–M-B7 were addressed; confirm against the remediation log).
- M-B8–M-B12 backend mediums.
- C5 deep rework: spatial index / canvas rendering for the per-pan heatmap scan (the accumulation bound is in; the O(n) pan scan remains).
- Frontend medium cluster + remaining a11y mediums (M-F16–18); then a formal axe/Lighthouse pass per #322.
- Small leftovers from earlier sessions: measure-union typing (AI storytelling P1), Ask-AI page overhaul (spec was lost — needs re-spec before build).

### P3 — Feature direction (after the above)

- `docs/feature-data-roadmap.md` (on the #348 branch) is the researched direction: flagship = statistically rigorous hotspot detection (SPF + Empirical Bayes), hospital/EMS undercount corrector, roadway inventory. Use its §5 tier list.
- `docs/data-storytelling-plan.md` (on the #349 branch): Lens B (VMT exposure) has a feasibility doc on the branch; Lenses A/C ship with #349.
- Open MEGA trackers: #291–#293, #300–#310, #322 (P1-important ones: #300 security, #301 race conditions, #303 test coverage, #322 finish line), plus feature issues #279, #280, #256.

## Local workspace cleanup (low stakes)

Untracked in the working tree: `docs/superpowers/plans/2026-05-20-sprint4-remaining.md` (May plan, largely superseded — R2 backup shipped; check if the day_of_week matview task is still wanted, then delete or commit), `backend/data/shn_raw.geojson` (highways raw data — likely should be gitignored), `frontend/playwright-report/` + `frontend/test-results/` (gitignore candidates), `This PC - Shortcut.lnk` (delete).

## Ground rules for the implementing agent

- Single prod environment; main auto-deploys. Never merge red CI.
- Never modify prod access controls (SSH keys, exposure) — reads/diagnostics fine, access changes are Jeff's.
- No AI attribution in commits or PRs. Additive changes; don't rewrite working code. No column-aligned formatting.
- `DEFAULT_YEARS`/`DEFAULT_SEVERITIES` must stay empty (no default filters on first visit).
