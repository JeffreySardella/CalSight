# Handoff — 2026-07-11

Consolidates a large multi-wave audit + remediation effort so the thread can be picked up
cleanly. Two PRs, one merged, one open. Full detail lives in the linked reports.

## PR status

- **#371 — merged & deployed to production.** The 2026-07-09 audit + fixes for all 7 highs,
  ~20 mediums/lows, and the completable backlog across issues #256/#291/#292/#293/#300/#303/#304/#370
  and feature #279. Deploy verified green end-to-end (migrations applied, backup-writability H5
  confirmed live).
- **#372 — open, CI green on all workflow jobs, awaiting review/merge.** 12 commits (see
  `git log origin/main..`). Contents, grouped:
  1. **Audit-backlog lows** (M8 freshness verification; L4/L5/M-B3/L-X1; frontend lows).
  2. **Feature:** Ask AI conversation export (Markdown/JSON) — #256/#293.
  3. **Reports:** `docs/deep-audit-2026-07-11.md`, `docs/runner-hardening-research-2026-07-11.md`.
  4. **Deep-audit fix wave:** D-1, S-1, SC-1/2/3, P-1, P-2, P-3, P-4, CSP/PII/misc.

  > #372 is large and spans several concerns. Consider splitting for review, e.g.: (a) audit-lows
  > + conversation export, (b) the reports, (c) the deep-audit fix wave. Merging deploys to prod.

## Verification standard used

Backend changes touching query logic were verified against a **real local Postgres** (unit +
integration): the last full run was **715 unit + 278 integration passing**, ruff clean. Frontend:
tsc + eslint (0 errors) + full vitest + `npm run build`, with generated `sw.js`/`_headers`
artifacts spot-checked. Two production defects were caught *only* by the DB-backed suite (a flaky
grounding test; a `with db.begin()` transaction fix that broke under the real session) — keep
running the integration suite, not just `-m "not integration"`, before merging query changes.

## Deep-audit fixes shipped in #372 (from `docs/deep-audit-2026-07-11.md`)

| ID | Fix |
|---|---|
| **D-1** | Per-capita map rate was ~18× low under a year filter (the site's default measure). `/api/demographics` now honors the date range → stable annual-average rate. |
| **S-1** | The #293 LLM spend cap was inert in prod (default 0); deploy now writes a non-zero `LLM_DAILY_REQUEST_BUDGET`. |
| **SC-1/2/3** | CI token scoped to `contents: read` (writes per-job); `npm audit` non-blocking; bandit scans `backend/etl/`; dependabot covers og-image; `etl-status.sh` fixed; stray root lockfile deleted. |
| **P-1** | Threadpool aligned to the DB pool + fast-fail `pool_timeout` (was 40 threads vs 10 connections → 30s stalls). |
| **P-2** | TTL caches on `/intersections`, `/corridors`, `/crashes/clusters`; `/stats/yoy-changes` statement timeout + default-year routed off `mv_crashes_by_year`. |
| **P-3** | Nightly derived-flag resync scoped to the reloaded years (weekly `force_refresh` stays full via `CALSIGHT_FORCE_REFRESH`); M10 `IS DISTINCT FROM` logic unchanged. |
| **P-4 / M4 / S-3 / D-5** | Memoized `fatalGradient`; SW `NetworkOnly` guard for raw-crash/PII responses; CSP `base-uri`/`object-src`/`form-action`; dynamic © year; `rate_limit.py` comment corrected. |

## OPEN — needs a decision, not more engineering

1. **D-2 (product):** `is_alcohol_involved` is TRUE for drug-only-impaired crashes (sobriety =
   `HBD-UNDER INFLUENCE` **OR** `UNDER_DRUG_INFLUENCE`), so drug-only crashes are labeled
   "alcohol-involved" and double-counted in the alcohol + drug metrics. Fix is ready to implement
   (alcohol-only flag + optional `is_impairment_involved` union) once the **definition** is
   confirmed. `backend/etl/backfill_derived.py:311-312,450-451`.
2. **D-3 (editorial):** Data-Stories hard-code headline stats (e.g. `r = 0.73` that contradicts
   the live Correlation Explorer) with some causal phrasing; the anomaly detector labels ordinary
   right-skew outliers "95% confidence / critical"; the correlation matrix prints Pearson r at
   n≥5 with no significance/causation caveat. Compute-from-live vs. badge-as-editorial is a
   framing call.
3. **CodeQL check-run (repo setting, not code):** the workflow's advanced CodeQL analysis
   succeeds and uploads, but the "CodeQL" check fast-fails every push — the signature of GitHub
   **default CodeQL setup being enabled and conflicting with the advanced/workflow setup**. Fix:
   Settings → Code security → Code scanning → disable **Default setup** (recommended, since the
   workflow has a custom config), *or* remove the CodeQL steps from `ci.yml`. Can't run both. All
   other CI jobs are green; this does not block the CI workflow conclusion or the deploy gate.

## Still-open engineering backlog (documented, not yet done)

- **Structural perf (long-term):** normalize+index the road columns (or an `mv_intersections`) so
  statewide street aggregation stops full-scanning 11M rows; incremental-by-year MV refresh;
  shared rate-limit store (Redis) so per-worker limits stop multiplying by 4.
- **Operator tasks (need prod/host access):** rotate the shared DB password; `systemctl` cleanup
  of the retired scheduler unit; run the real `pg_restore` drill; confirm edge HSTS/heartbeat.
- **Runner hardening** (`docs/runner-hardening-research-2026-07-11.md`): OIDC for R2 to drop
  persisted secrets; JIT/ephemeral runners; isolate the runner from the prod host + ship image
  digests; bump `actions/checkout` to v7 (fork-PR-refusal default).

## Prior-audit carryovers not yet addressed

H-S2 (dev services on 0.0.0.0), H-I5 (secrets persist on runner), M-B2 (ETL in gunicorn worker),
M-B11 (per-worker limiter state), M-F1/F14, M-I2/I6, plus assorted lows — see the remediation
addenda in `docs/audit-2026-07-09.md` and `docs/deep-audit-2026-07-11.md`.
