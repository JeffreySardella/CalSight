fastapi==0.141.1
uvicorn[standard]==0.52.4
gunicorn==23.0.0
sqlalchemy==2.0.52
alembic==1.19.2
psycopg2-binary==2.9.13
httpx==0.28.1
python-dotenv==1.2.3
pydantic-settings==2.15.0
openai==2.54.0
shapely==2.1.2
# Tract point-in-polygon for etl/compute_tract_crashes.py (no PostGIS on
# the DB host). geopandas pulls pandas + pyproj; pyogrio is the shapefile
# reader. All three ship manylinux wheels with GDAL/PROJ bundled, so the
# Dockerfile needs no extra apt packages.
geopandas==1.1.4
pyogrio==0.13.0
slowapi==0.1.10
apscheduler==3.11.3
boto3>=1.43.91,<2.0.0
sentry-sdk[fastapi]==2.69.1
