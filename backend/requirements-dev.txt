-r requirements.txt
ruff==0.15.22
pytest==9.1.1
pytest-asyncio==1.4.0
# Build-time only: etl/build_tract_topojson.py regenerates the committed
# frontend/public/ca-tracts.topo.json. Not in the runtime image.
topojson==1.10
